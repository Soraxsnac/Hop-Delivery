<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="VehiculosMAUI.Views.AgregarCervezaPage"
             Title="Nueva Cerveza"
             BackgroundColor="#F5F5F5">

    <ScrollView>
        <StackLayout Margin="20" Spacing="15">
            <Label Text="Nombre:" FontAttributes="Bold" />
            <Entry x:Name="NombreEntry" Placeholder="Ej. Stout Imperial" />

            <Label Text="Tipo:" FontAttributes="Bold" />
            <Entry x:Name="TipoEntry" Placeholder="Ej. Oscura" />

            <Label Text="ABV (% de Alcohol):" FontAttributes="Bold" />
            <Entry x:Name="ABVEntry" Placeholder="Ej. 8.5" Keyboard="Numeric" />

            <Label Text="IBU (Amargor):" FontAttributes="Bold" />
            <Entry x:Name="IBUEntry" Placeholder="Ej. 40" Keyboard="Numeric" />

            <Label Text="Descripción:" FontAttributes="Bold" />
            <Editor x:Name="DescripcionEditor" Placeholder="Notas de sabor..." HeightRequest="80" />

            <Label Text="URL de la Imagen:" FontAttributes="Bold" />
            <Entry x:Name="ImagenEntry" Placeholder="https://..." />

            <Button Text="Guardar Cerveza" 
                    BackgroundColor="#27AE60" 
                    TextColor="White" 
                    FontAttributes="Bold"
                    CornerRadius="10"
                    Margin="0,20,0,0"
                    Clicked="OnGuardarClicked" />
        </StackLayout>
    </ScrollView>
</ContentPage>